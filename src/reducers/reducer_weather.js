export default function (state = [], action) {
    // console.log('Action receved', action);
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
    return state;   
}